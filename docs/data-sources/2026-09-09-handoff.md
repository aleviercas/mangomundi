# HANDOFF — research "moneda volátil → margen de remesas" + implementación pendiente
### 2026-09-09 — para pegar en un chat nuevo con red habilitada hacia GitHub

## 0. Por qué existe este handoff

Esta sesión hizo todo el research y la auditoría posible, pero **no puede escribir en el
repo** (`github.com`/`api.github.com`/`raw.githubusercontent.com`/`codeload.github.com`
siguen bloqueados por el proxy de red de `bash_tool` en esta conversación, incluso después
de que Alejandro habilitó "Allow network egress" a mitad de sesión — el cambio no tomó
efecto en caliente). Supabase sí funcionó de punta a punta acá.

**Repo:** `aleviercas/mangomundi`, rama de trabajo: `kayakclone`
**Supabase project_id:** `ttqalbexpquzobrdyvgx`
**PAT de GitHub:** vive en el project knowledge de Claude "mangomundi" (NO en este chat) —
recomendado rotarlo antes de usarlo, seguía expuesto en un archivo de texto plano. Se
confirmó que tiene lectura; **el scope de escritura no está confirmado todavía**.

## 1. Regla de oro antes de escribir cualquier cosa (pedida explícitamente por Alejandro)

1. `git fetch origin kayakclone` (o `GET /repos/aleviercas/mangomundi/branches/kayakclone`)
   **inmediatamente antes** de tocar nada — nunca usar un SHA visto antes en la conversación.
2. Clonar y trabajar con git real (`git clone` + `checkout kayakclone`), no
   `create_or_update_file` archivo por archivo vía API.
3. `git diff HEAD origin/kayakclone --stat` antes de editar — si origin avanzó en los
   archivos que se van a tocar, parar y avisar, no asumir que la copia local está al día.
4. Push normal, nunca `--force` ni `--force-with-lease` salvo pedido explícito de Alejandro.
5. Después del push, volver a traer `origin/kayakclone` y comparar línea por línea que el
   diff sea exactamente el cambio esperado.

Mismo criterio para Supabase: `list_migrations` antes de `apply_migration`, usar el
timestamp real que devuelve (no fabricarlo — un timestamp fabricado ya causó un incidente
de CI el 25/08/2026).

## 2. Los 4 archivos de research de esta ronda (ya escritos, en `/mnt/user-data/outputs/`)

1. **`RECONCILIACION-fx-rates-vs-research-2026-09-09.md`** — auditó las 24 filas de
   `fx_rates` cargadas en research anterior (v16-v25) contra la fórmula real de combinación
   spread+fee (`spread = 1 − (1−costo_real)/(1−fee/monto)`). **Conclusión: las 24 ya están
   bien cargadas, no requieren ningún UPDATE.**
2. **`research-findings-2026-09-09-v26.md`** — research nuevo, con 6 secciones:
   - Tanzania→EEUU/Alemania (OFX, comisión $0): **medido y listo para cargar**
     (8.07% y 8.14% respectivamente).
   - Asia Central/Cáucaso: los datos ya cargados en Supabase son placeholders genéricos
     (2%/2.5% parejo, batch "World Bank RPW Q3 2025") sin medición viva.
   - Venezuela: **medido dos veces** (200 USD y 500 USD, mismo resultado consistente) —
     único proveedor disponible es Remitly. El "tipo medio" que muestra Monito está
     anclado al dólar oficial BCV, no al paralelo — corregido con `dolarapi.com`.
   - Causa raíz de por qué Asia Central/Cáucaso no tiene cobertura: el mercado real está
     dominado por proveedores CEI (KoronaPay/Unistream/Contact) que Monito no indexa, y
     **KoronaPay Europe está en proceso de cierre de su autorización regulatoria** — ni
     siquiera el proveedor nativo dominante es medible hoy. Recomendación: marcar esta
     región como "cobertura no verificable por disrupción de mercado", no seguir
     insistiendo con Monito.
   - Fuente nueva descubierta y validada: **`dolarapi.com`/`ve.dolarapi.com`** — API
     pública, gratis, sin auth, con tipo de cambio oficial Y paralelo en tiempo real para
     Argentina, Venezuela, Chile, Uruguay, México, Bolivia, Brasil, Colombia. Resuelve el
     "problema estructural del ARS" que quedó abierto en el instructivo original (Sección 6)
     — y lo mismo aplica a VES, con una brecha oficial/paralelo mucho más grande (~13%).
3. **`PLAN-IMPLEMENTACION-2026-09-09.md`** — plan concreto: qué cargar ya, qué no, y en
   qué orden. Resumen:
   - **Cargar:** Tanzania→EEUU (8.07%), Tanzania→Alemania (8.14%), ambos comisión $0.
   - **Cargar con nota metodológica:** EEUU→Venezuela/Remitly, recalculado contra el dólar
     paralelo (`ve.dolarapi.com`) da ≈ -0.45% a -0.66% (dos mediciones), no el -14% "sin
     sentido" que da si se usa el tipo medio de Monito (anclado al oficial BCV). Confirmar
     con Alejandro si se carga el spread negativo tal cual o se trata como "sin margen
     visible" — es una decisión de producto, no solo de datos.
   - **NO cargar todavía:** nada de Asia Central/Cáucaso (sin fuente medible hoy).
   - **Pendiente, no urgente:** recalcular los corredores ARS ya cargados
     (Chile→España, Argentina→EEUU, Bolivia×5) contra `dolarapi.com` — la brecha
     oficial/blue de Argentina hoy es angosta (~2%) así que el efecto es menor, pero vale
     la pena antes de dar esos números por definitivos.
4. **Este handoff.**

## 3. Lo que este research encontró en el repo (leído vía TinyFish, solo lectura)

- El repo **es público**, `docs/README.md` es el índice de documentación.
- **Ninguno de los archivos de research de "moneda volátil"** (`INSTRUCTIVO-carga-v16-a-v24.md`,
  `CONCLUSIONES-moneda-volatil-margen-v16-v25.md`, `research-findings-*`) está en el repo —
  vivieron solo como archivos sueltos en esta conversación de chat. Hay que subirlos.
- Existe `docs/data-sources/2026-08-diagnostico-arquitectura-proveedores-corredores.md`
  (24-ago-2026, sí está en el repo) que toca el mismo problema desde otro ángulo
  (arquitectura de `fx_rates`, por qué desaparecen proveedores en ciertos corredores) y
  **ya proponía `dolarapi.com`/`esjs-dolar-api`** como solución al problema del ARS — esta
  ronda de research es la primera vez que esa fuente se usó y validó en la práctica. Los
  dos documentos deberían cruzar referencias.

## 4. Qué hacer en el chat nuevo, en orden

1. Verificar la red: `curl -s -i https://api.github.com/repos/aleviercas/mangomundi` — si
   devuelve 403 `host_not_allowed`, la config todavía no tomó efecto, avisar a Alejandro.
2. Si la red funciona: seguir la Sección 1 de este handoff (fetch → diff → clone → cambios
   → push sin force → verificación).
3. Subir los archivos de research a `docs/data-sources/` (ver Sección 2 del `PLAN-IMPLEMENTACION`
   para la lista exacta) — esto no toca código ni Supabase, es el cambio de menor riesgo,
   conviene primero y por separado del resto.
4. Agregar cross-references entre el nuevo research y
   `docs/data-sources/2026-08-diagnostico-arquitectura-proveedores-corredores.md`.
5. Actualizar `docs/README.md` para que los archivos nuevos aparezcan listados.
6. Recién con confirmación explícita de Alejandro, aplicar el UPDATE de los 3 corredores
   listos en Supabase (Sección 2, punto 3 de este handoff) vía `apply_migration`, con
   timestamp real de `list_migrations`.
7. Verificar que `fx_rates` sigue en 920 filas + los nuevos agregados (no menos — sería
   señal de que algo se pisó).

## 5. Estado de Supabase al cierre de esta sesión

`fx_rates`: 920 filas, sin cambios de esta sesión (solo lectura). Nada escrito.
